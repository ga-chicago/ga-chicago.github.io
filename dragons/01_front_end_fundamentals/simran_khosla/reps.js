/// 1. Define a function max()
function max(x,y) {
  if (x>y) {
    return x;
    console.log(x);
  } else {
    return y;
    console.log(y);
  } else if (x===y) {
    return 'both are the same';
    console.log(x);
  }
}
//2.
function isVowel(char)
var char = char.toLocaleLowerCase;
  {
  return char === 'a' || char === 'e' || char === 'i' ||
  char === 'o' || char === "u" || false;
};


///3. sum + multiply
var num = [1, 2, 3, 4];
var total = array[0]
function sum(array) {
  for (var i = 0 ); i < array.length; i++) {
    total += array[i]
  }
}
