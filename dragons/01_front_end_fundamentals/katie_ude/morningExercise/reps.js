// Morning Rep 1.

function max(a, b) {
  if (a > b) {
    return a;
  }
  else {
      return b;
  }
}
max(31, 22);

// Morning Rep 2.

function char() {
  if (char == 'a'||'e'||'i'||'o'||'u') {
      return 'true'
  }
  else {
    return 'false'
  }
}

// Morning Rep 3.

var array = [1, 2, 3, 4];
function sum(array) {
  var total = 0;
  for (var i = 0; i < array.length; i++) {
      total = array[i];
  }
    console.log(total);
  }

function multiply(array) {
  var total = array[0];
  console.log(total);
  for (var i = 1; i < array.length; i++) {
    total = total * array [i];
  }
    console.log(total);
}
