window.onload () {

  var guessOne = prompt('Please pick a random number from 0-15');
  var reply = randomNumber();

  function hint(guessOne) {
    if (randomNumber <= 13) {
      prompt('Nice try, but your response needs to be a larger number!');
      function hint();
    }
    else if (randomNumber > 15) {
      prompt('Well played, but please try a number that is smaller!');
      function hint();
    }
    else {
      prompt('You picked the winner! You are amazing!');
    }
    function hint(guessOne)
  }





} //end of window.onload
