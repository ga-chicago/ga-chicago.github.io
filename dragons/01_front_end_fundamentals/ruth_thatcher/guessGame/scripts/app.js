
window.onload = function() {
  //

 var randomNumber= Math.floor(Math.rando() * 10 +1)
 var number = prompt('Guess a number between 1 and 10! ');
    if {number > randomNumber result;
        return var number2 = prompt('That\'s too high. Guess again!');
    }
  var number2 = prompt('That\'s too high. Guess again!');
    var number3 = prompt('That\'s too low. Guess again!');
      var number4 = prompt('What is a number ');


  var contentContainer = document.getElementById('content');

  contentContainer.innerHTML = username + ' is on a mission to ' + mission;
}

function statusReport(message) {
  console.log(message);
  return message;
}
