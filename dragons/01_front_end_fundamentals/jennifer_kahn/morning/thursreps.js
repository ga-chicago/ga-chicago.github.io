
function max(a, b) {
  }
  return(x,y)
  if(max.length > = 3 )
return
  )


Write a function that takes a characte
return char.toLowercase()
 === 'a' || char === 'e' ||
 char === 'i' || char ===
 'o' || char === 'u';
 }



function sum(array){
  var total = 0;
  for(var i=1; i<array.length;
    i++{
      total = total * array[i];
    }

for(var * )

function(multiply)
