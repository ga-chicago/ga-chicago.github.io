//windows.onload=(){

//// write out an array of five foods
// loop through the array and console.log each item with a message

// create an object with your name with 3 to 4 attributes including one array
// create a method for your object that accepts an argument that is an array and
//loops through the array and console.logs the array with a message!

// create a button in html
// style your button give it a border, color, radius, etc....(Maybee put it
//  inside a div and center the dive on your screen)
// using javascript console.log my button is working when it is clicked
//Invoke your objects method using your button by passing in the object
//property that includes its arrray

//// write out an array of five foods
var foods = ['cheese', 'yogurt', 'milk', 'pudding', 'cream']

// loop through the array and console.log each item with a message

  for (var i = 0, i<foods.length, i++) {
  console.log(foods [i] + ' is a great food');
  }

// create an object with your name with 3 to 4 attributes including one array

var Ruth= {
  name: 'Ruth',
  hair: 'Brunette',
  eyes: 'blue',
 myFaveTV: ['sopranos', 'bobs burgers']
 }

window.onload =function() {
var btn =document.getElementByID('btn');
// create a method for your object that accepts an argument that is an array and
//loops through the array and console.logs the array with a message!
  ruth.myFaveTV= function('arr')
    for (var i=0, i <arr.length, i++) {
      console.log(arr[i] + ' i love tv')
    }

  // create a button in html

<button type='btn' name='button'> HELLO </button>

// style your button give it a border, color, radius, etc....(Maybee put it
//  inside a div and center the dive on your screen)

// using javascript console.log my button is working when it is clicked
//Invoke your objects method using your button by passing in the object
//property that includes its arrray

btn.onclick= function() {
  console.log('button is functional')
}
}
